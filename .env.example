# Optional. Leave empty in dev — Vite proxies /helix/api to Django :8000.
# VITE_API_BASE_URL=http://127.0.0.1:8000

# App base path (default /helix/ for nginx). Use / for root hosting.
# VITE_BASE_PATH=/helix/
